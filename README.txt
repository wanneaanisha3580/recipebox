Quickstart
1) In phpMyAdmin, run sql/schema.sql
2) Copy 'recipebox' folder to your web root as /recipebox
3) Open http://localhost/recipebox
4) Register a user and login. After login you will be redirected home.
5) Add a recipe with an image. The image is uploaded to /uploads and the recipe appears on the homepage.
